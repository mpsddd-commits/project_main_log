- MVC2 구조
- Models : Model / (front) : View / apis : Controller / utils : 유틸 
---
backend/
└── src/
    └── utils/                # 외부 라이브러리 및 공통 모듈 관리
        ├── setting.py        # 환경 변수 관리 (Env, Config)
        ├── logset.py         # Log 관리, 파일 저장 (JSON 형식 등)
        ├── kafkasv.py        # Kafka 서비스 (Pd: 생산, Cs1: 이메일 발송, Cs2: AI 처리)
        ├── rediscl.py        # Redis 클라이언트 설정 및 연결
        ├── websc.py          # 실시간 로그 화면 출력 및 알람 처리 (WebSocket 등)
        ├── db.py             # 데이터베이스(DB) 연결 및 세션 관리
        ├── file.py           # 파일 업로드 및 다운로드 로직
        ├── tokenset.py       # 인증 토큰 관리 (Access Token, Refresh Token)
        └── crypto.py         # 데이터 암호화 및 복호화 처리
    └── apis/
        ├── user.py           # 회원 관리 (/put : 회원 가입, /patch : 회원 수정, /delete : 회원 탈퇴)
        ├── auth.py           # 인증 관리 (/post : 로그인, /delete : 로그아웃, /get : 비밀번호 찾기 메일 발송)
        ├── invite.py         # /put : 초대 관리
        └── board.py          # /get : KPI 정보 조회
    └── models/
        ├── model.py          # BaseModel 통해 만들어진 model들 
        ├── user.py           # 회원 관련
        ├── auth.py           # 인증 관련
        ├── invite.py         # 초대 관련
        └── board.py          # KPI 정보 조회

---
- 비밀번호 찾기 로직

1. 이메일에 임시 비밀번호 발송 << url 고정 문제점이 없어서 좋음.
2. 링크를 주고 새 비밀번호 설정 화면 // 별로인 방법
3. 소켓 쓰는 기술: 1회용 url

: **합리적**으로 1번 선택 why? 2번은 보안 이슈 / pass앱같은 공인 인증 수단이 없으므로 1번 선택

- 메일 쏘는 경우

1. 초대장 3가지
    1-1 사내 직원 초대
    1-2 컨설턴트 초대 
        1-2-1 신규 컨설턴트
        1-2-2 기존 컨설턴트 추가
2. 임시 비밀번호 찾기 메일

정리

-- 개인적으로 필요 MSA 게이트웨이 관련 내용 확인  fastapi 폴더 추가 STUDY38
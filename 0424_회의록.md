DB쪽에서 전달해주는 Back쪽 관련사항 
DB설계로 컴퍼니_컴퍼니 협업사 관계 기능 추가, depth가 있고, 요청자 코드 (원청사. 협력사측 양쪽 신청하게 한다) 로직
1. 회갑되어있으면 내부 알림을 통해 협업사 관계 승인(웹소켓으로 알려주는 기능구현?).
2. 회갑 안되어있으면 메일로 전송시켜 (메일에 입장 링크만 보낸다.) 회원 가입 하게 한 후 신청코드 보내기. 관계가 자동 처리 될 수 있게 할 것인가? 자동화 하려면 초대장 보내는 사람 키 값이 필요하다. 
trubleshooting:
1) 회원 가입유도 후 하게 되면 사업자번호등을 기입 해야 하므로 오래걸릴 것이라 예상해서 바로 가입하지 않고 나중에 가입하자 그냥 링크로 와서 휴먼 에러 발생 가능성 있음. 그래서 자동화를 하면 안된다. 
회원 가입은 기업에게 보낸다.
메일 초대장 루트 관리 1. 사용자 2. 기업 보낼때만약 해결을 하게 된다면 어떤게 있을까? 
링크 통해 들어가면 로그 연결

# 협력사 가입 유도 후 승인관련 사항 :
메일에 레디스로 키값으로 컴퍼니 상하 아이디 값+depth 을 보낸 값으로 초대링크를 보내 회갑 시키기고 자동 연결 시키는 로직 구현시 
 기본 로직 : 링크 상하회사 아이디 값을 보낸 값으로 초대링크를 보내 => 들어왔다 => 회원 가입 후 => 원청사에서는 보냈을 때 회원 가입이 되었다는 것이 확인 되었다 표시 뜸. => 승인버튼을 누르면 => 원청사 상태값에 승인되었다는 연결 로직이다. 

 TS1(HE): i메일에 링크를 들어왔다. => 가입할 내용이 너무 많아서 포기 => 나중에 따로 사이트에 들어와서 회갑 => 레디스에서 보낸 키 값 없이 가입되어 원청사에서는 회원 가입 및 승인 할 내용 확인 불가 => 체결 승인/ 거절은 로직상 구현 가능(시간 제한이거나 한번 접속 후 어느정도 뒤에 링크 통한 가입진행 X시)
 TS2(HE): 현재 invite 컬럼으로 보내는 로직 구현시 보내는 email은 회사정보 테이블이 아니라 사용자 정보 테이블로 초대하는 로직으로 구현 되었는데 만약 초대 후 가입시 확인이 불가능한 로직이 될 것이라 판단된다.
 TS3(HE): 모가 가입유도 후 봤는데 별로야(고객 변심으로) 탈퇴 하면 관계 테이블 상태가 계속 대기로 표시 될 것이다 front에서 볼 때 어떻게 할 것인가? invite 삭제 처리시 기록들은 어떻게 할 것인가? 계약은 처리 되었으나 기록이 문제가 있다.

대안 필요.





2) gateway사용 따로따로 진행해서 하나의 nginx로 묶어 연결 가능하지만 한쪽으로 모아서 하는 이유는? 휴먼 에러의 내용을 한쪽으로 모아서 진행 할 수 있기 때문에 가능하다. 

3. 협업사 관계 끊기 로직 구현 + 협업 관계 끝나면 delete_yn이 아니라 그냥 테이블 삭제 처리 하는 것으로 이야기 나옴.

이슈테이블 추가 => 부모자식 관계 내용 테이블 생성 

ESG담당자가 초대인원 및 허락 인원 리스트 화면 추가 구현하는 로직 구상안?

관계 수동 변경과 끊고 연결하는 로직 추가. 계약사에서 탈퇴 기능.(마이페이지에서 탈퇴) 추가.

Front 회원(초대?)추가 부분

한번에 리스트 보여주고 필요 부분 보내주면 이를 response 할 로직 추가.



----
0424 내부 회의
RESTFUl한 method가 더 필요할 것으로 예상 .apis에서 .py 하나 추가 할 예정. 명칭과 세부 메써드 논의 후 확정.


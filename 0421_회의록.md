# 안건

## 재로그인 방식
 이메일에 초대링크 보냄 or 인증 키 발급 => 링크를 타게 되면 비밀번호 생성 창을 띄우고 비밀번호 생성시킴 => 재로그인
만약 오타로 잘못 인증과 인가가 되었을때 대처법

컨설팅회사 관련 내용 -> 로그인 로직 추가 => 로그인 후 a회사,b회사 선택란 만들고 사용끝난 회사 지워지는 기능 추가 (정렬기준 선택방법: 최근, 가나다순할지 선정)

## 백 기능 추가
- 로그인
 초대 링크 보낼때 db에서 사용자 있는지 없는지 확인 후 1. 회원이 있으면 메일로 새로운 업무 추가 되었다는 메일 발송. 2. 회원이 아니라면 user 회원가입 페이지 이동 시키기 3. 회사가 없다면 회원가입 페이지 보내서 ESG담당자 회원가입 페이지 이동

- 회원가입
ESG담당자아이디(어드민)
사업자등록증 사진 읽기

- 공공데이터로 사업자번호 대조 api주소 
(https://www.data.go.kr/data/15081808/openapi.do) 

- OCR 사용 예시 방안

```bash
서비스무료                  범위    (월 단위)       특징
Google Cloud Vision     매월 첫 1,000건 무료        한글 인식률이 매우 높고 범용적입니다.
Naver CLOVA OCR         약 100건~300건 내외         한국 사업자등록증 양식에 특화된 모델을 별도로 제공하여 정확도가 가장 뛰어납니다. (신규 가입 시 크레딧 제공 방식)
OCR.space               매월 25,000건 무료          무료 제공량이 압도적이지만, 한글 인식률은 Google/Naver에 비해 다소 떨어질 수 있습니다.

회원가입 인증버튼에서 1. 메일주소 중복여부확인 + 사업자번호 중복확인 2.결과값 전송(어떤 부분이 문제였는지)  3.  api로 전송 및 확인
```
- websocket reference

https://fastapi.tiangolo.com/advanced/websockets/

# 파일 디렉토리 1차 구성

```bash
backend/
└── src/
    └── utils/                # 외부 라이브러리 및 공통 모듈 관리
        ├── setting.py        # 환경 변수 관리 (Env, Config)
        ├── logset.py         # Log 관리, 파일 저장 (JSON 형식 등)
        ├── kafkasv.py        # Kafka 서비스 (Pd: 생산, Cs1: 이메일 발송, Cs2: AI 처리)
        ├── rediscl.py        # Redis 클라이언트 설정 및 연결
        ├── websc.py          # 실시간 로그 화면 출력 및 알람 처리 (WebSocket 등)
        ├── db.py             # 데이터베이스(DB) 연결 및 세션 관리
        ├── file.py           # 파일 업로드 및 다운로드 로직
        ├── tokenset.py       # 인증 토큰 관리 (Access Token, Refresh Token)
        └── crypto.py         # 데이터 암호화 및 복호화 처리
    └── apis/
        ├── user.py           # 회원 관리 (/put : 회원 가입, /patch : 회원 수정, /delete : 회원 탈퇴)
        ├── board.py          # /get : KPI 정보 조회 
        ├── auth.py           # 인증 관리 (/post : 로그인, /delete : 로그아웃)
        └── invite.py         # /put : 초대 관리
```

최종 BACKEND 회의 후 정리중 사항
1. endpoint 들 작성 + RESTFUL 메소드 정의 및 정리
2. 파일 디렉토리 구조 아직 진행중